---
description: GUI WebView2 StatusWindow — leia a skill python-gui
globs:
  - python/gui.py
  - python/ui/**
alwaysApply: false
---

# Python GUI

Ao alterar `python/gui.py` ou `python/ui/` (painel, bridge, DnD, rename, menus):

Leia e siga **`.cursor/skills/python-gui/SKILL.md`**.

Ações de persistência vivem em `python/nm/` — não duplicar I/O de config na GUI.
