---
description: Core C++ (ICMP, CLI, CMake) — leia a skill cpp-core
globs: cpp/**/*
alwaysApply: false
---

# C++ core

Ao alterar `cpp/` (headers, src, CMake, testes CTest):

Leia e siga **`.cursor/skills/cpp-core/SKILL.md`**.

Fase 1 = CLI only (sem tray/GUI/toast). Schema compartilhado → skill `config-schema`.
