---
description: Core C++ (ICMP, CLI, CMake, UI Win32) — leia a skill cpp-core
globs: cpp/**/*
alwaysApply: false
---

# C++ core

Ao alterar `cpp/` (headers, src, CMake, testes CTest):

Leia e siga **`.cursor/skills/cpp-core/SKILL.md`**.

Inclui CLI, ICMP, bandeja, toast e painel Win32. Schema compartilhado → skill `config-schema`.
